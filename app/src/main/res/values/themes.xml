
<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.MoviesDBKotlin" parent="Theme.AppCompat.DayNight.DarkActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryVariant">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <!-- Secondary brand color. -->

        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
    </style>

    <style name="ThemeForAppPadding" parent="ThemeOverlay.MaterialComponents.Light">
        <item name="android:padding">8dp</item>
    </style>

    <style name="ThemeForAppTextTitleConfig" parent="ThemeOverlay.MaterialComponents.Light">
        <item name="android:textSize">30sp</item>
        <item name="android:textStyle">bold</item>
        <item name="android:textColor">@color/white</item>
        <item name="fontFamily">@font/headland_one</item>
    </style>

    <style name="ThemeForAppTextOverviewConfig" parent="ThemeOverlay.MaterialComponents.Light">
        <item name="android:textSize">18sp</item>
        <item name="android:textStyle">bold</item>
        <item name="android:textColor">#ffffff</item>
        <item name="fontFamily">@font/headland_one</item>
        <item name="android:padding">20dp</item>
        <item name="android:layout_marginTop">5dp</item>
    </style>

    <style name="ThemeForRatingBar" parent="ThemeOverlay.MaterialComponents.Light">

        <item name="android:isIndicator">true</item>
        <item name="android:max">10</item>
        <item name="android:numStars">10</item>
        <item name="android:progressBackgroundTint">#FD605E5E</item>
        <item name="android:stepSize">0.5</item>
    </style>

    <style name="ThemeForAppTextTitleOverviewConfig" parent="ThemeOverlay.MaterialComponents.Light">
        <item name="android:textSize">18sp</item>
        <item name="android:textStyle">bold</item>
        <item name="android:textColor">@color/white</item>
        <item name="android:gravity">center_horizontal</item>
        <item name="fontFamily">@font/headland_one</item>
        <item name="android:layout_marginTop">10dp</item>
    </style>

    <style name="ThemeForAppTextRecycler" parent="ThemeOverlay.MaterialComponents.Light">
        <item name="android:textSize">17sp</item>
        <item name="android:textStyle">bold</item>
        <item name="android:textColor">@color/white</item>
        <item name="fontFamily">@font/headland_one</item>


    </style>
</resources>